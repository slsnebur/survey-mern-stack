import RegisterBox from '../../components/register';
import Start from '../../routes/Start';
import React from "react";

function RegisterRoute(props) {
    return(
        <Start content={RegisterBox}/>
    )
}

export default RegisterRoute;
